# Tree_file:

### class TreeNode

### build_expression_tree - функция создающая дерево по выражению

### evaluate - функция вычисляющая резултат по дереву

### count_operations - функция подсчитывающая колличество операций в дереве
